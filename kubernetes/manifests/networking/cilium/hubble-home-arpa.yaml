apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: hubble-home-arpa
  namespace: kube-system
spec:
  secretName: hubble-home-arpa-tls
  issuerRef:
    name: homelab-ca-issuer
    kind: ClusterIssuer
  dnsNames:
    - hubble.home.arpa