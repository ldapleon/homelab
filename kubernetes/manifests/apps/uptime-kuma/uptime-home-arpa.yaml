apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: uptime-home-arpa
  namespace: uptime-kuma
spec:
  secretName: uptime-home-arpa-tls
  issuerRef:
    name: homelab-ca-issuer
    kind: ClusterIssuer
  dnsNames:
    - uptime.home.arpa