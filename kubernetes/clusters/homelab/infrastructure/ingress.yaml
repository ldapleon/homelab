apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization

metadata:
  name: ingress
  namespace: flux-system

spec:
  interval: 10m

  dependsOn:
    - name: networking

  sourceRef:
    kind: GitRepository
    name: flux-system

  path: ./kubernetes/manifests/ingress

  prune: true
  wait: true